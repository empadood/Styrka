# Styrka Development Guidelines

- Use `rem` units instead of `px` for CSS sizing and spacing unless a pixel value is required for a precise visual effect.
- Use Flex or Grid with gap in `rem` instead of `margin` or padding `padding` to create space between elements.
